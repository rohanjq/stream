# Real secrets — copy the .example file and fill in your own values.
demo/ai.env
demo/youtube.env
config/ai.env
config/signals.env
config/youtube.env
demo/token.json
token.json
.env
*.env.local

# Local scratch/debug output accumulated during development
demo/*.png
demo/frame.png

# Large binaries fetched at build time now (see demo/Containerfile) —
# these were only here as a workaround for one machine's specific
# corporate proxy and aren't portable/needed elsewhere.
demo/piperpkg/

# Generated caches and durable runtime data
**/__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
runtime/

# OS/editor noise
.DS_Store
.claude/
demo/smelter/
